{
  "name": "frontline",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run",
    "deploy": "twilio-run deploy"
  },
  "dependencies": {
    "@twilio/runtime-handler": "1.1.3",
    "airtable": "^0.11.1",
    "axios": "^0.21.1",
    "twilio": "^3.56"
  },
  "devDependencies": {
    "twilio-run": "^3.2.2"
  },
  "engines": {
    "node": "12"
  }
}
